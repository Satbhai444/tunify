import TrackPlayer, { 
  Capability, 
  Event, 
  State, 
  RepeatMode, 
  AppKilledPlaybackBehavior 
} from 'react-native-track-player';

export const RNTP = {
  TrackPlayer,
  Capability,
  Event,
  State,
  RepeatMode,
  AppKilledPlaybackBehavior,
};

export const isAvailable = true;
